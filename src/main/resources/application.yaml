# ========================================
# = Spring Boot Configuration
# ========================================

spring:
  application:
    name: personal-finance-manager
    description: Personal Finance Manager Application

  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true

  datasource:
    # MySQL Database (for production)
    url: jdbc:mysql://103.228.36.73:3306/personal_finance_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: quanly
    password: ProjectOop123@
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 20000
      idle-timeout: 300000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  thymeleaf:
    cache: false
    encoding: UTF-8
    mode: HTML
    prefix: classpath:/templates/
    suffix: .html


server:
  port: 8083
  servlet:
    session:
      timeout: 30m
